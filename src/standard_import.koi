import std::logging::info
import std::logging::debug
import std::logging::config
import std::logging::warn
import std::logging::fatal

fun main() -> int {
    call info("Hello, World!")
    call debug("Hello, World!")
    call config("Hello, World!")
    call warn("Hello, World!")
    call fatal("Hello, World!")

    return 0
}